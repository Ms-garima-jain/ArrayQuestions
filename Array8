// Find maximum even number.

const arr=[1, 2, 3,4, 5]
  let max =arr[0]
   for ( let i =0; i<arr.length; i++)
   {

    if (arr[i]%2==0)
    {
          if (arr[i]>max)
            max= arr[i]
    }
   }
    console.log ("max even number"+ max)
