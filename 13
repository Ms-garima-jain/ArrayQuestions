let arr = [3,7,9,14,18,23];
  let mineven=Infinity
   for (let i =0; i<arr.length ; i++)
   {
        if (arr[i]%2==0 && arr[i]< mineven)
        {
            mineven=arr[i]
        }
   }
    console.log (mineven)
  
